interface IAddress {
    address: string;
    city: string;
    country: string;
    zip: string;
}

const Address = {
    address: String,
    city: String,
    country: String,
    zip: String,
};

export { IAddress, Address };
